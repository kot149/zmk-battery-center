name: "Tauri Build & Release"

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag in the format vX.Y.Z (must match package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml)"
        required: true
        type: string
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  verify-versions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Verify versions match (package.json / tauri.conf.json / Cargo.toml / git tag)
        run: bun scripts/verify_versions.ts --tag "${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}"

  # create-release:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v6
  #       with:
  #         fetch-depth: 0

  #     - name: get-version-from-package-json
  #       id: get_version
  #       run: echo "version=$(jq -r '.version' package.json)" >> $GITHUB_OUTPUT

  #     - name: Create Release
  #       run: |
  #         gh release create v${{steps.get_version.outputs.version}} --generate-notes --draft --prerelease
  #       env:
  #         GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}

  build-and-release-tauri:
    needs: verify-versions
    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS arm
          - platform: "macos-latest"
            args: "--target aarch64-apple-darwin"
          # macOS x64
          - platform: "macos-latest"
            args: "--target x86_64-apple-darwin"
          # macOS universal
          # - platform: "macos-latest"
          #   args: "--target universal-apple-darwin"

          # Windows x64
          - platform: "windows-latest"
            args: ""

          # Ubuntu x64
          - platform: "ubuntu-latest"
            args: "--target x86_64-unknown-linux-gnu"
          # Ubuntu arm64
          - platform: "ubuntu-24.04-arm"
            args: "--target aarch64-unknown-linux-gnu"

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          # Those targets are only used on macos runners so it's in an `if` to slightly speed up windows and linux builds.
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: install dependencies (ubuntu x64 only)
        if: matrix.platform == 'ubuntu-latest' # This must match the platform value defined above.
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: install dependencies (ubuntu arm64 only)
        if: matrix.platform == 'ubuntu-24.04-arm' # This must match the platform value defined above.
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf xdg-utils

      - name: Install frontend dependencies
        run: bun install --frozen-lockfile

      - name: Restore bun cache
        uses: actions/cache@v5
        with:
          path: |
            node_modules
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install cargo-about
        run: cargo install cargo-about

      - name: Generate licenses
        run: bun scripts/generate_licenses.ts --skip-verify

      # If tagName and releaseId are omitted tauri-action will only build the app and won't try to upload any assets.
      - uses: tauri-apps/tauri-action@v0
        id: tauri
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__ # the action automatically replaces \_\_VERSION\_\_ with the app version.
          releaseName: 'v__VERSION__'
          releaseDraft: true
          prerelease: true
          args: ${{ matrix.args }}
